{
  "name": "agent-os",
  "version": "1.0.0",
  "private": true,
  "description": "Agent OS — Mission Control for Free Claude Code (the Sovereign Stack 'Command' pillar)",
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "install:all": "npm install",
    "build": "npm run build -w client && npm run build -w server",
    "build:client": "npm run build -w client",
    "build:server": "npm run build -w server",
    "dev:server": "npm run dev -w server",
    "dev:client": "npm run dev -w client",
    "start": "npm run build && npm start -w server",
    "serve": "npm start -w server",
    "cli": "node bin/agentos.mjs",
    "test": "npm test -w server"
  },
  "bin": {
    "agentos": "bin/agentos.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
