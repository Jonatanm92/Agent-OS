{
  "name": "jarful",
  "version": "0.1.0",
  "private": true,
  "description": "Jarful — the honest recipe box. Save recipes from any link, caption or photo; plan the week; one grocery list for the whole household.",
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "dev": "node --watch server.mjs",
    "test": "node --test test/*.test.mjs"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.128.0"
  },
  "engines": {
    "node": ">=20"
  }
}
