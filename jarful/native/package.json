{
  "name": "jarful-native",
  "private": true,
  "type": "module",
  "description": "Capacitor shells that package the Jarful web app for the App Store and Google Play.",
  "scripts": {
    "build:web": "node build.mjs",
    "sync": "node build.mjs && cap sync",
    "android": "node build.mjs && cap sync android && cap open android",
    "ios": "node build.mjs && cap sync ios && cap open ios"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/core": "^7.0.0",
    "@capacitor/ios": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
